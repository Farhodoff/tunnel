# Tunnel Service

# Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# Client
aiohttp>=3.9.0
websockets>=12.0

# Common
python-multipart>=0.0.6
redis>=5.0.0  # optional: distributed rate limiting (memory fallback if missing/down)
prometheus_client>=0.19.0  # /metrics (manual fallback if missing)

# SSL/TLS
cryptography>=41.0.0

# Dev
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0  # ASGI transport for REST/E2E tests
